---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: scopes.coralogix.com
spec:
  group: coralogix.com
  names:
    kind: Scope
    listKind: ScopeList
    plural: scopes
    singular: scope
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Scope is the Schema for the scopes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ScopeSpec defines the desired state of a Coralogix Scope.
            properties:
              defaultExpression:
                description: Default expression to use when no filter matches the
                  query. Until further notice, this is limited to `true` (everything
                  is included) or `false` (nothing is included). Use a version tag
                  (e.g `<v1>true` or `<v1>false`)
                enum:
                - <v1>true
                - <v1>false
                type: string
              description:
                description: Description of the scope. Optional.
                type: string
              filters:
                description: Filters applied to include data in the scope.
                items:
                  description: ScopeFilter defines a filter to include data in a scope.
                  properties:
                    entityType:
                      description: Entity type to apply the expression on.
                      enum:
                      - logs
                      - spans
                      - unspecified
                      type: string
                    expression:
                      description: Expression to run.
                      type: string
                  required:
                  - entityType
                  - expression
                  type: object
                type: array
              name:
                description: Scope display name.
                type: string
            required:
            - defaultExpression
            - filters
            - name
            type: object
          status:
            description: ScopeStatus defines the observed state of Coralogix Scope.
            properties:
              id:
                type: string
            required:
            - id
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
